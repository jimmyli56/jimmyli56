# from gettext import find
# bad = "cokes"
# user = "all cokes is better than pepsi"

# while (bad in user):
#     index = user[:index] + ("x" * len(bad)) + user[index + len(bad):]
# print(user)


print(sum)